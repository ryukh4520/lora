# LoRA 학습 데모 프로젝트 설계안

## 🎯 프로젝트 개요
RTX 3070 (8GB VRAM) 환경에서 작은 LLM을 LoRA(Low-Rank Adaptation)를 통해 효율적으로 파인튜닝하는 데모 프로젝트

---

## 📋 하드웨어 제약사항 분석

### 현재 환경
- **GPU**: RTX 3070
- **VRAM**: 8GB
- **제약사항**: 
  - 대형 모델(7B 이상) 전체 파인튜닝 불가능
  - 배치 사이즈 제한
  - 시퀀스 길이 제한 필요

### 최적화 전략
- LoRA를 통한 파라미터 효율적 학습 (학습 파라미터 < 1%)
- 4-bit/8-bit 양자화 (QLoRA) 활용
- Gradient Checkpointing 활용
- 작은 모델 선택 (1B~3B 파라미터)

---

## 🏗️ 프로젝트 구조

```
lora/
├── README.md                    # 프로젝트 설명서
├── requirements.txt             # 의존성 패키지
├── config/
│   ├── model_config.yaml       # 모델 설정
│   └── training_config.yaml    # 학습 설정
├── data/
│   ├── raw/                    # 원본 데이터
│   ├── processed/              # 전처리된 데이터
│   └── prepare_dataset.py      # 데이터 전처리 스크립트
├── src/
│   ├── __init__.py
│   ├── model.py                # 모델 로딩 및 LoRA 설정
│   ├── dataset.py              # 데이터셋 클래스
│   ├── trainer.py              # 학습 로직
│   └── utils.py                # 유틸리티 함수
├── scripts/
│   ├── train.py                # 학습 실행 스크립트
│   ├── inference.py            # 추론 테스트 스크립트
│   └── merge_lora.py           # LoRA 가중치 병합 스크립트
├── notebooks/
│   └── demo.ipynb              # 데모 노트북
├── outputs/
│   ├── checkpoints/            # 체크포인트
│   ├── logs/                   # 학습 로그
│   └── merged_models/          # 병합된 모델
└── tests/
    └── test_model.py           # 단위 테스트
```

---

## 🤖 추천 모델 선택

### Option 1: GPT-2 Small (124M) ⭐ 추천 (입문용)
- **파라미터**: 124M
- **VRAM 사용량**: ~2GB (LoRA + 8bit)
- **장점**: 빠른 학습, 안정적, 한국어 토크나이저 커스터마이징 가능
- **단점**: 성능 제한적
- **예상 학습 시간** (10K 샘플, 3 epochs):
  - **총 소요 시간**: ~30-45분
  - **초당 샘플 처리**: ~10-12 samples/sec
  - **Epoch당 시간**: ~10-15분
  - **추천 사용 케이스**: 빠른 프로토타이핑, LoRA 개념 학습, 실험

### Option 2: Llama-2-1B / TinyLlama-1.1B ⭐⭐ 추천
- **파라미터**: 1.1B
- **VRAM 사용량**: ~4-5GB (QLoRA + 4bit)
- **장점**: 최신 아키텍처, 좋은 성능
- **단점**: GPT-2보다 느림
- **예상 학습 시간** (10K 샘플, 3 epochs):
  - **총 소요 시간**: ~1.5-2시간
  - **초당 샘플 처리**: ~4-5 samples/sec
  - **Epoch당 시간**: ~30-40분
  - **추천 사용 케이스**: 실용적인 성능이 필요한 프로젝트, 균형잡힌 선택

### Option 3: Phi-2 (2.7B) ⭐⭐⭐ 최고 추천
- **파라미터**: 2.7B
- **VRAM 사용량**: ~6-7GB (QLoRA + 4bit)
- **장점**: Microsoft 개발, 뛰어난 성능, 8GB VRAM에서 학습 가능
- **단점**: 상대적으로 무거움
- **예상 학습 시간** (10K 샘플, 3 epochs):
  - **총 소요 시간**: ~2.5-3.5시간
  - **초당 샘플 처리**: ~2.5-3 samples/sec
  - **Epoch당 시간**: ~50-70분
  - **추천 사용 케이스**: 최고 품질이 필요한 프로덕션 환경, 복잡한 태스크

**최종 추천**: **Phi-2** (성능과 VRAM 효율성의 균형)

---

### 📊 학습 시간 비교표 (RTX 3070 기준)

| 모델 | 파라미터 | VRAM | 1 Epoch | 3 Epochs | Samples/sec | 시간당 처리량 |
|------|----------|------|---------|----------|-------------|---------------|
| **GPT-2 Small** | 124M | ~2GB | 10-15분 | 30-45분 | 10-12 | ~36K-43K |
| **TinyLlama-1.1B** | 1.1B | ~4-5GB | 30-40분 | 1.5-2시간 | 4-5 | ~14K-18K |
| **Phi-2** | 2.7B | ~6-7GB | 50-70분 | 2.5-3.5시간 | 2.5-3 | ~9K-11K |

**참고사항**:
- 위 시간은 `max_seq_length=512`, `batch_size=1`, `gradient_accumulation_steps=16` 기준
- 실제 시간은 데이터 복잡도, 시퀀스 길이, 시스템 상태에 따라 ±20% 변동 가능
- `max_seq_length`를 256으로 줄이면 학습 시간 ~40% 단축 가능
- `gradient_accumulation_steps`를 8로 줄이면 학습 시간 ~20% 단축 (단, 성능 저하 가능)

---

### 🎯 상황별 모델 선택 가이드

#### 빠른 실험 및 학습이 목적이라면
→ **GPT-2 Small** (30-45분)
- LoRA 개념 이해
- 파이프라인 테스트
- 하이퍼파라미터 실험

#### 하루 안에 실용적인 결과가 필요하다면
→ **TinyLlama-1.1B** (1.5-2시간)
- 여러 번 실험 가능 (하루 4-5회)
- 괜찮은 성능
- VRAM 여유 있음

#### 최고 품질의 결과가 필요하고 시간 여유가 있다면
→ **Phi-2** (2.5-3.5시간)
- 밤새 학습 가능
- 프로덕션 레벨 품질
- 복잡한 instruction following

#### 대용량 데이터셋 (50K+ 샘플)을 학습한다면
→ **GPT-2 Small** 또는 **TinyLlama-1.1B**
- Phi-2는 50K 샘플 기준 12-17시간 소요
- 실용성 고려 필요

---

## 📚 학습 데이터셋 선택

### Option 1: 한국어 대화 데이터
- **데이터셋**: AI Hub 한국어 대화 요약, KoAlpaca
- **용도**: 챗봇, 대화형 AI

### Option 2: 한국어 지시사항 따르기
- **데이터셋**: KoAlpaca, Korean Instruction Dataset
- **용도**: Instruction Following

### Option 3: 커스텀 도메인 데이터
- **데이터셋**: 사용자 정의 (예: 특정 분야 QA)
- **용도**: 특화된 작업

**데모용 추천**: **KoAlpaca** (한국어 Instruction 데이터, 공개 데이터셋)

---

## 🔧 기술 스택

### 핵심 라이브러리
1. **transformers** (Hugging Face): 모델 로딩 및 학습
2. **peft** (Parameter-Efficient Fine-Tuning): LoRA 구현
3. **bitsandbytes**: 4-bit/8-bit 양자화
4. **accelerate**: 분산 학습 및 최적화
5. **datasets**: 데이터셋 로딩 및 전처리
6. **wandb** (선택): 학습 모니터링

### LoRA 설정 파라미터
```yaml
lora_r: 8                    # LoRA rank (낮을수록 파라미터 적음)
lora_alpha: 16               # LoRA scaling factor
lora_dropout: 0.05           # Dropout 비율
target_modules:              # LoRA 적용 레이어
  - q_proj
  - v_proj
  - k_proj
  - o_proj
```

### 학습 하이퍼파라미터 (8GB VRAM 최적화)
```yaml
# 모델 로딩
load_in_4bit: true           # 4-bit 양자화 (QLoRA)
bnb_4bit_compute_dtype: float16
bnb_4bit_quant_type: "nf4"

# 학습 설정
batch_size: 1                # Gradient Accumulation으로 보완
gradient_accumulation_steps: 16  # 실질적 배치 사이즈 = 16
max_seq_length: 512          # 시퀀스 길이 (메모리 절약)
learning_rate: 2e-4
num_epochs: 3
warmup_steps: 100
gradient_checkpointing: true # 메모리 절약

# 최적화
optim: "paged_adamw_8bit"    # 메모리 효율적 옵티마이저
fp16: true                   # Mixed Precision Training
```

---

## 📝 단계별 구현 계획

### Phase 1: 환경 설정 및 기본 구조 (30분)
1. 프로젝트 디렉토리 생성
2. `requirements.txt` 작성
3. 가상환경 설정 및 패키지 설치
4. 기본 설정 파일 작성

**산출물**:
- 프로젝트 폴더 구조
- `requirements.txt`
- `config/model_config.yaml`
- `config/training_config.yaml`

---

### Phase 2: 데이터 준비 (30분)
1. 샘플 데이터셋 다운로드 (KoAlpaca 또는 커스텀)
2. 데이터 전처리 스크립트 작성
3. 토크나이저 테스트
4. 데이터셋 클래스 구현

**산출물**:
- `data/prepare_dataset.py`
- `src/dataset.py`
- 전처리된 데이터 샘플

---

### Phase 3: 모델 및 LoRA 설정 (45분)
1. 모델 로딩 함수 구현 (4-bit 양자화)
2. LoRA 설정 적용
3. 모델 메모리 사용량 확인
4. 간단한 추론 테스트

**산출물**:
- `src/model.py`
- `src/utils.py`
- 모델 로딩 검증 스크립트

---

### Phase 4: 학습 파이프라인 구현 (1시간)
1. Trainer 클래스 구현
2. 학습 루프 작성
3. 체크포인트 저장 로직
4. 로깅 및 모니터링 설정

**산출물**:
- `src/trainer.py`
- `scripts/train.py`
- 학습 로그 예시

---

### Phase 5: 추론 및 평가 (30분)
1. 추론 스크립트 작성
2. LoRA 가중치 병합 스크립트
3. 간단한 평가 메트릭 구현
4. 데모 노트북 작성

**산출물**:
- `scripts/inference.py`
- `scripts/merge_lora.py`
- `notebooks/demo.ipynb`

---

### Phase 6: 테스트 및 최적화 (30분)
1. 전체 파이프라인 테스트
2. VRAM 사용량 모니터링
3. 하이퍼파라미터 튜닝
4. 문서화 완성

**산출물**:
- `README.md`
- `tests/test_model.py`
- 최종 학습 결과 리포트

---

## 🎓 예상 학습 시간 및 성능

### 학습 시간 (Phi-2 기준)
- **데이터셋 크기**: 10,000 샘플
- **Epoch**: 3
- **예상 시간**: 2-3시간 (RTX 3070)
- **체크포인트 크기**: ~10-20MB (LoRA 어댑터만)

### 메모리 사용량
- **모델 로딩**: ~3-4GB (4-bit 양자화)
- **학습 중**: ~6-7GB (Gradient Checkpointing 포함)
- **여유 메모리**: ~1-2GB

---

## 🚀 실행 예시

### 1. 환경 설정
```bash
# 가상환경 생성
python -m venv venv
source venv/bin/activate  # Linux/Mac
# venv\Scripts\activate  # Windows

# 패키지 설치
pip install -r requirements.txt
```

### 2. 데이터 준비
```bash
python data/prepare_dataset.py --dataset koalpaca --num_samples 10000
```

### 3. 학습 실행
```bash
python scripts/train.py \
    --model_name microsoft/phi-2 \
    --dataset_path data/processed/train.json \
    --output_dir outputs/checkpoints \
    --num_epochs 3 \
    --batch_size 1 \
    --gradient_accumulation_steps 16
```

### 4. 추론 테스트
```bash
python scripts/inference.py \
    --base_model microsoft/phi-2 \
    --lora_weights outputs/checkpoints/final \
    --prompt "한국의 수도는 어디인가요?"
```

### 5. LoRA 가중치 병합
```bash
python scripts/merge_lora.py \
    --base_model microsoft/phi-2 \
    --lora_weights outputs/checkpoints/final \
    --output_dir outputs/merged_models/phi2-lora-merged
```

---

## 📊 모니터링 및 디버깅

### VRAM 모니터링
```bash
# 실시간 GPU 사용량 확인
watch -n 1 nvidia-smi
```

### 학습 로그 확인
- TensorBoard 또는 Weights & Biases 사용
- 손실(Loss) 감소 추이 확인
- Gradient Norm 모니터링

### 일반적인 문제 해결
1. **OOM (Out of Memory)**:
   - `batch_size` 감소
   - `max_seq_length` 감소
   - `gradient_accumulation_steps` 증가
   - `lora_r` 감소

2. **학습 불안정**:
   - Learning rate 감소
   - Warmup steps 증가
   - Gradient clipping 적용

3. **성능 부족**:
   - 더 많은 데이터 사용
   - Epoch 수 증가
   - `lora_r` 증가 (메모리 허용 범위 내)

---

## 🎯 성공 기준

### 최소 목표
- [x] 8GB VRAM 내에서 학습 완료
- [x] LoRA 어댑터 저장 및 로딩 성공
- [x] 기본적인 추론 동작 확인

### 권장 목표
- [x] 학습 손실(Loss) 지속적 감소
- [x] 추론 품질 향상 확인
- [x] 체크포인트 관리 및 재개 가능

### 고급 목표
- [x] 다양한 프롬프트에서 일관된 성능
- [x] 병합된 모델 배포 가능
- [x] 하이퍼파라미터 최적화 완료

---

## 📖 참고 자료

1. **LoRA 논문**: [LoRA: Low-Rank Adaptation of Large Language Models](https://arxiv.org/abs/2106.09685)
2. **QLoRA 논문**: [QLoRA: Efficient Finetuning of Quantized LLMs](https://arxiv.org/abs/2305.14314)
3. **Hugging Face PEFT**: https://github.com/huggingface/peft
4. **Phi-2 모델**: https://huggingface.co/microsoft/phi-2
5. **KoAlpaca 데이터셋**: https://github.com/Beomi/KoAlpaca

---

## 💡 다음 단계

이 설계안을 검토하신 후, 다음과 같이 진행하겠습니다:

1. **Phase 1부터 순차적으로 구현** 시작
2. 각 Phase별로 코드 작성 및 테스트
3. 문제 발생 시 즉시 디버깅 및 최적화
4. 최종적으로 완전히 동작하는 데모 프로젝트 완성

**질문사항**:
- 특정 도메인이나 작업에 집중하고 싶으신가요? (예: 챗봇, 요약, QA 등)
- 데이터셋은 공개 데이터셋을 사용하시겠습니까, 아니면 커스텀 데이터를 준비하시겠습니까?
- Wandb 같은 실험 추적 도구를 사용하시겠습니까?

준비되시면 Phase 1부터 코드 작성을 시작하겠습니다! 🚀
